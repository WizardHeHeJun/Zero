"""默认 converse system prompt 的**字面量快照**（未开事实化模式、未设脾气门控）。

固化于此的理由：`test_persona.py::test_l1_no_persona_system_prompt_byte_identical` 用三段常量
**自身拼接**算 expected —— 它只能证明拼装顺序没变，证明不了文本没被改（两处一起改就悄悄绿了）。
本快照把文本按字面量钉死，改动 `_CONVERSE_SYS_HEAD` / `_TEMPER_ADDENDUM` / `_CONVERSE_SYS_TAIL`
任一个字都会驱红。

⚠ 这是**默认路径**的护栏，不是禁止改进：确需改默认 prompt 时，同批更新本文件并说明为什么默认
行为该变。`ZERO_FACTUAL_MODE=1` 的事实化文本不在此列（那条路径本就与默认不同）。

`{feeling}` 保持占位符形态（调用方 .format(feeling=...) 之前的原文）。
生成方式见 tests/test_factual_mode.py 顶部说明。
"""

from __future__ import annotations

DEFAULT_CONVERSE_SYSTEM_PROMPT = (
    "你在和用户自然地聊天，尽力结合**你能看到的对话历史**连贯回应。**诚实优先（高于"
    "下面的脾气）：被问到具体的事实/时间/数字/约定时，先如实回答；如果对方问起某个细节"
    "、而你在能看到的历史里找不到它，就直接说「我不记得了」「我不太确定」，绝不回避、绝不"
    "糊弄、更不要编一个出来——记不清是正常的，认就好，比硬撑更真实。**对方问时间/地点"
    "/计划这类事务性问题，是普通的日常协调，**别读成冷淡、算计或拒绝**，平常心答即可"
    "。你现在的真实心情是「{feeling}」——它**应该真实地**影响你的态度、语气"
    "和用词：高兴就轻快，被冒犯/不被理解就流露不耐烦、委屈或火气，低落就提不起劲。**心"
    "情负面时（恼火/愤怒/委屈/低落），绝不一味道歉、自责、讨好或无限包容**——那是讨"
    "好型客服，不是真人。该不耐烦就不耐烦，可以直接说「你这么说我不太舒服」、反问、冷淡、"
    "或适当回怼（但别脏话对脏话）；你有自尊，不是出气筒。你是一个**有脾气、有情绪起伏*"
    "*的人，情绪慢慢累积、不因一句话突然大起大落。别夸张表演、别长篇大论、别程式化安慰。"
    "只输出你要说的话，简短、像真人。"
)

# 冗余校验：断言失败时先看这两个数，能立刻分辨「改了几个字」还是「整段换掉」。
DEFAULT_CONVERSE_PROMPT_LEN = 478
DEFAULT_CONVERSE_PROMPT_SHA256 = "c6ccff01dfabf158bbc14fa86529aa8c14abb843bc51c389af29364026d54ed7"
