# Shell 脚本必须用 LF，否则带 \r 的行尾会让 sh 执行失败
*.sh text eol=lf

# 其余文本文件交给 git 自动处理换行
* text=auto

# unified diff 对行尾敏感：CRLF 检出会让 git apply 到 LF 目标仓（Bert-VITS2）时上下文失配
# ——须放在 * text=auto 之后（gitattributes 后行覆盖前行）
*.patch -text
